// Dashboard — list of the current user's documents + upload control.
//
// Implemented in Task 6:
//   * Mounts -> GET /documents
//   * Renders a list of <DocumentCard> with status badge (processing/indexed/failed).
//   * <UploadDropzone> -> POST /documents/upload (multipart), refreshes list.

export default function Dashboard() {
  return <div>Dashboard (scaffold)</div>;
}
