// Register page — email + password (min 8 chars) form, calls auth.register(),
// auto-logs-in on success. Implemented in Task 6.

export default function Register() {
  return <div>Register (scaffold)</div>;
}
