// Ask page — question input, answer card with cited badges, source cards,
// query metrics, and history list. Mirrors the project-1 UI.
//
// Implemented in Task 6:
//   * Question form -> POST /query
//   * Answer card with [filename, p.PAGE] citation badges
//   * Source cards with similarity score bars
//   * Refusal banner when the model emits the refusal phrase
//   * GET /query/history pinned in a sidebar/panel

export default function Ask() {
  return <div>Ask (scaffold)</div>;
}
