"""Sentence-aware chunking.

To be ported from project 1 (engineering-rag/src/chunking.py) in Task 3.
The chunking logic itself does not change between projects — same
500-token target, same 50-token overlap, same greedy sentence packing.
"""
# TODO: copy from engineering-rag/src/chunking.py
